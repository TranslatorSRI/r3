openapi: 3.0.2
info:
  title: Redis-REST with Referencing for Chemical Normalization
  description: 'Given a chemical substance, return similar chemical substances.</br>This site utilizes RDKit to determine similar substances based on thier SMILES values.'
  version: '1.0.0'
  contact:
    email: powen@renci.org
  termsOfService: http://robokop.renci.org:7055/tos?service_long=Redis-REST+with+Referencing&provider_long=the+Translator+Consortium
  license:
    name: MIT
    url: 'https://opensource.org/licenses/MIT'
tags:
  - name: Interfaces(s)
paths:
  /get_similar:
    get:
      tags:
        - Interfaces(s)
      summary: 'Get similar chemical substances for the chemical substance requested.'
      description: ''
      parameters:
        - in: query
          name: key
          schema:
            type: array
            items:
              type: string
            example:
              -  CHEBI:77287
      responses:
        '200':
          description: Results
          content:
            application/json:
              schema:
                type: object
                example:
                  request: response